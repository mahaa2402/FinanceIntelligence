import 'package:flutter/foundation.dart'
    show defaultTargetPlatform, kIsWeb, TargetPlatform;

// This is a placeholder class for demo purposes
// In a real app, this would contain the actual Firebase configuration options
class DefaultFirebaseOptions {
  // This dummy class is used to maintain compatibility with the original code structure
  // but doesn't actually interact with Firebase since we're running in demo mode

  static void initializeApp() {
    // No-op implementation for demo mode
    print('Demo mode: No Firebase initialization needed');
  }
}
